# SkyPilot task: score an exported issue #402 HF checkpoint on all three
# frozen retrieval-augmented variant benchmarks.
#
# Launch from the repository root:
#   sky launch -c dna-exp402-offline-step1000 --down \
#     scripts/issue402_offline_eval_sky.yaml \
#     --env CHECKPOINT_URI=gs://.../hf/step-1000 \
#     --env OUTPUT_URI=gs://.../offline-evals/step-1000 \
#     --env CODE_REVISION=<full-commit-sha>

name: dna-exp402-offline-eval

resources:
  infra: aws/us-east-2
  accelerators: A10G:1
  disk_size: 100
  labels:
    project: dna
  any_of:
    - use_spot: true
    - use_spot: false

workdir: .

envs:
  CHECKPOINT_URI: ""
  OUTPUT_URI: ""
  CODE_REVISION: ""
  BATCH_SIZE: "16"
  N_BOOTSTRAP: "1000"

file_mounts:
  ~/.config/gcloud/application_default_credentials.json: ~/.config/gcloud/application_default_credentials.json

setup: |
  set -euxo pipefail
  export PATH="$HOME/google-cloud-sdk/bin:$HOME/.local/bin:$PATH"

  if ! command -v uv >/dev/null 2>&1; then
    curl -LsSf https://astral.sh/uv/install.sh | sh
  fi

  if ! command -v gcloud >/dev/null 2>&1; then
    curl -fsSL https://sdk.cloud.google.com > /tmp/gcloud-install.sh
    bash /tmp/gcloud-install.sh --disable-prompts --install-dir="$HOME"
  fi

  uv sync --frozen --no-dev

run: |
  set -euxo pipefail
  export PATH="$HOME/google-cloud-sdk/bin:$HOME/.local/bin:$PATH"
  export GOOGLE_APPLICATION_CREDENTIALS="$HOME/.config/gcloud/application_default_credentials.json"
  export WANDB_DISABLED=true

  test -n "$CHECKPOINT_URI"
  test -n "$OUTPUT_URI"
  test "${#CODE_REVISION}" -eq 40
  test "$BATCH_SIZE" -gt 0
  test "$N_BOOTSTRAP" -gt 0
  nvidia-smi

  CHECKPOINT_DIR="$HOME/issue402-checkpoint"
  RESULTS_DIR="$HOME/issue402-offline-results"
  mkdir -p "$CHECKPOINT_DIR" "$RESULTS_DIR"
  gcloud storage rsync "$CHECKPOINT_URI" "$CHECKPOINT_DIR" --recursive
  test -s "$CHECKPOINT_DIR/config.json"
  test -s "$CHECKPOINT_DIR/model.safetensors"
  test -s "$CHECKPOINT_DIR/tokenizer.json"

  for BENCHMARK in mendelian_traits complex_traits sge; do
    uv run --no-dev python scripts/issue402_score_rag_hf.py \
      --benchmark "$BENCHMARK" \
      --split test \
      --model "$CHECKPOINT_DIR" \
      --model-source "$CHECKPOINT_URI" \
      --code-revision "$CODE_REVISION" \
      --output-dir "$RESULTS_DIR/$BENCHMARK" \
      --batch-size "$BATCH_SIZE" \
      --n-bootstrap "$N_BOOTSTRAP" \
      --device cuda
    test -s "$RESULTS_DIR/$BENCHMARK/metrics.parquet"
    test -s "$RESULTS_DIR/$BENCHMARK/manifest.json"
    gcloud storage rsync \
      "$RESULTS_DIR/$BENCHMARK" \
      "$OUTPUT_URI/$BENCHMARK" \
      --recursive
  done
