# SkyPilot task: score the exact first 1,842 paired training rows used by issue
# #402's bounded online-harness parity smoke.
#
# Launch from the repository root with a commit containing this task:
#   sky launch -c dna-exp402-parity-p104m-final --down \
#     scripts/issue402_parity_eval_sky.yaml \
#     --env CHECKPOINT_URI=gs://.../hf/step-7628 \
#     --env OUTPUT_URI=gs://.../parity/train-first-1842 \
#     --env CODE_REVISION=<full-commit-sha>

name: dna-exp402-offline-parity

resources:
  infra: aws/us-east-2
  accelerators: A10G:1
  disk_size: 100
  labels:
    project: dna
  any_of:
    - use_spot: true
    - use_spot: false

workdir: .

envs:
  CHECKPOINT_URI: ""
  OUTPUT_URI: ""
  CODE_REVISION: ""
  MAX_ROWS: "1842"
  BATCH_SIZE: "16"

file_mounts:
  ~/.config/gcloud/application_default_credentials.json: ~/.config/gcloud/application_default_credentials.json

setup: |
  set -euxo pipefail
  export PATH="$HOME/google-cloud-sdk/bin:$HOME/.local/bin:$PATH"

  if ! command -v uv >/dev/null 2>&1; then
    curl -LsSf https://astral.sh/uv/install.sh | sh
  fi

  if ! command -v gcloud >/dev/null 2>&1; then
    curl -fsSL https://sdk.cloud.google.com > /tmp/gcloud-install.sh
    bash /tmp/gcloud-install.sh --disable-prompts --install-dir="$HOME"
  fi

  uv sync --project snakemake/analysis/evals_v2 --locked --no-dev

run: |
  set -euxo pipefail
  export PATH="$HOME/google-cloud-sdk/bin:$HOME/.local/bin:$PATH"
  export GOOGLE_APPLICATION_CREDENTIALS="$HOME/.config/gcloud/application_default_credentials.json"
  export WANDB_DISABLED=true

  test -n "$CHECKPOINT_URI"
  test -n "$OUTPUT_URI"
  test "${#CODE_REVISION}" -eq 40
  test "$MAX_ROWS" -eq 1842
  test $((MAX_ROWS % 2)) -eq 0
  test "$BATCH_SIZE" -gt 0
  nvidia-smi

  CHECKPOINT_DIR="$HOME/issue402-parity-checkpoint"
  RESULTS_DIR="$HOME/issue402-parity-results"
  mkdir -p "$CHECKPOINT_DIR" "$RESULTS_DIR"
  gcloud storage rsync "$CHECKPOINT_URI" "$CHECKPOINT_DIR" --recursive
  test -s "$CHECKPOINT_DIR/config.json"
  test -s "$CHECKPOINT_DIR/model.safetensors"
  test -s "$CHECKPOINT_DIR/tokenizer.json"

  uv run --project snakemake/analysis/evals_v2 --locked --no-dev python scripts/issue402_score_rag_hf.py \
    --benchmark mendelian_traits \
    --split train \
    --max-rows "$MAX_ROWS" \
    --model "$CHECKPOINT_DIR" \
    --code-revision "$CODE_REVISION" \
    --output-dir "$RESULTS_DIR" \
    --batch-size "$BATCH_SIZE" \
    --n-bootstrap 0 \
    --device cuda

  for ARTIFACT in documents.parquet variants.parquet metrics.parquet manifest.json; do
    test -s "$RESULTS_DIR/$ARTIFACT"
  done
  gcloud storage rsync "$RESULTS_DIR" "$OUTPUT_URI" --recursive
