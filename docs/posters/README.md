# Posters

Each subdirectory here is a **frozen historical artifact** — the version
of the poster that was actually presented at a given event. Both the
`.typ` source and the rendered `.pdf` should stay as they were on the
day of the event; do not edit after submission.

For a new poster, create a new subdirectory (e.g. `neurips26/`).
