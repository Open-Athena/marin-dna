[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "marin-dna-genome-selection"
version = "0.0.1"
description = "Locked runtime and Python package for MarinDNA genome selection"
requires-python = ">=3.13,<3.14"
dependencies = [
    "marin-dna",
    "pandas>=2.0,<3.0",
    "pyarrow>=21.0.0",
    "snakemake",
    "snakemake-storage-plugin-s3>=0.3.6",
]

[dependency-groups]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
]

[tool.uv]
required-version = "==0.11.31"
fork-strategy = "fewest"
environments = [
    "sys_platform == 'darwin'",
    "sys_platform == 'linux'",
]

[tool.uv.sources]
marin-dna = { path = "../../..", editable = true }

[tool.hatch.build.targets.wheel]
packages = ["src/marin_dna_genome_selection"]

[tool.pytest.ini_options]
testpaths = ["tests"]
