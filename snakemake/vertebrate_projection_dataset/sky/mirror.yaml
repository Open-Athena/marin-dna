name: vertebrate-multiz-mirror

# One-off, resumable bootstrap of the 74.7 GB pinned UCSC MultiZ source set.
# Launch only when the versioned S3 prefix is missing objects:
#   sky launch -c vertebrate-multiz-mirror \
#     snakemake/vertebrate_projection_dataset/sky/mirror.yaml

resources:
  infra: aws/us-east-2
  instance_type: c6i.4xlarge
  disk_size: 100
  labels:
    project: dna

workdir: .

setup: |
  set -euxo pipefail

  if [ ! -d "$HOME/miniforge3" ]; then
    curl -fsSL -o /tmp/miniforge.sh https://github.com/conda-forge/miniforge/releases/latest/download/Miniforge3-Linux-x86_64.sh
    bash /tmp/miniforge.sh -b -p "$HOME/miniforge3"
    rm /tmp/miniforge.sh
  fi
  export PATH="$HOME/.local/bin:$PATH"
  if [ "$(uv --version 2>/dev/null || true)" != "uv 0.11.31" ]; then
    curl -LsSf https://astral.sh/uv/0.11.31/install.sh | sh
  fi
  test "$(uv --version)" = "uv 0.11.31"
  export PATH="$HOME/skypilot-runtime/bin:$HOME/miniforge3/bin:$HOME/.local/bin:$PATH"
  conda config --set channel_priority strict
  uv sync --project snakemake/vertebrate_projection_dataset --locked

run: |
  set -euxo pipefail
  export PATH="$HOME/skypilot-runtime/bin:$HOME/miniforge3/bin:$HOME/.local/bin:$PATH"
  command -v aws

  cd snakemake/vertebrate_projection_dataset

  uv run --locked snakemake mirror_multiz_bootstrap \
    --profile workflow/profiles/default \
    --cores 1 \
    --rerun-incomplete \
    --printshellcmds
