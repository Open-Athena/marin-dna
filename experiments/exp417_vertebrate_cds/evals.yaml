# Frozen terminal-checkpoint VEP overlay for issue #417.
# Loaded after snakemake/analysis/evals_v2/config/config.yaml: lists replace the
# base registries while all other inference defaults remain unchanged.

split: test

datasets:
  - name: mendelian_traits
    hf_revision: 4aed58e50c5dea0b878a665007af2ef9e5108e9f
    score_protocol: minus_llr
  - name: sge
    hf_revision: 225d3d1ea32a4af547891b13c33b5e92a5aae849
    score_protocol: minus_llr
    eval_protocol: sge

models:
  - name: exp417-cds-mammals-only-step-4999
    gcs_path: gs://marin-us-east5/checkpoints/dna-exp417-cds-mammals-only-p255m-b2m-5k/2026.08.01/hf/step-4999
    window_size: 255
    datasets:
      - mendelian_traits
      - sge
  - name: exp417-cds-combined-vertebrates-step-4999
    gcs_path: gs://marin-us-east5/checkpoints/dna-exp417-cds-combined-vertebrates-p255m-b2m-5k/2026.08.01/hf/step-4999
    window_size: 255
    datasets:
      - mendelian_traits
      - sge

# The base config registers optional off-`all` analyses against its large model
# registry. Replacing `models` requires clearing those inherited references.
nuc_dep:
  models: []
  loci: {}
umap_embeddings:
  models: []
ll_gap:
  models: []
  datasets: []
probe:
  models: []
