# exp402 fixed-layout DNA tokenizer

Immutable eight-token character vocabulary for issue #402. `[BOS]` is also
CLS, `[SEQ]` is an atomic sequence separator, no EOS is defined, and raw `N`
maps to `[UNK]`.

